namespace ConsoleSnake.Services
{
    public interface IHighScoreStore
    {
        int Load();
        void Save(int hiScore);

        // New: history support
        int[] LoadHistory();
        void SaveHistory(int[] scores);
        void Clear();
    }
}
