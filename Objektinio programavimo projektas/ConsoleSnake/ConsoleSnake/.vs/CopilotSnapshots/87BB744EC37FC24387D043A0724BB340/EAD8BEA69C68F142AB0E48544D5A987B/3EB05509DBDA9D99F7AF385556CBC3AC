namespace ConsoleSnake.Domain
{
    // value-type => lyginimas automatiškai pagal X,Y
    public readonly record struct Point(int X, int Y)
    {
        public Point Move(Point direction) => new(X + direction.X, Y + direction.Y);
    }
}
