/*#include <iostream>
using namespace std;

int suskaičiuotiPerimetra(int krastine) {
    return 4 * krastine;
}

void suskaiciuotiPlota(int krastine, int& plotas) {
    plotas = krastine * krastine;
}

int main() {
    int krastine, perimetras, plotas;

    cout << "Iveskite kvadrato krastine: ";
    cin >> krastine;

    perimetras = suskaičiuotiPerimetra(krastine);
    cout << "Kvadrato perimetras: " << perimetras << endl;

    suskaiciuotiPlota(krastine, plotas);
    cout << "Kvadrato plotas: " << plotas << endl;

    return 0;
}
*/

/*#include <iostream>
using namespace std;

double celsiusToFahrenheit(double tempCelsius) {
    return (tempCelsius * 9.0 / 5.0) + 32.0;
}

void fahrenheitToCelsius(double tempFahrenheit, double& tempCelsius) {
    tempCelsius = (tempFahrenheit - 32.0) * 5.0 / 9.0;
}

int main() {
    double tempCelsius, tempFahrenheit;

    cout << "Įveskite temperatūrą Celsijumi: ";
    cin >> tempCelsius;
    tempFahrenheit = celsiusToFahrenheit(tempCelsius);
    cout << "Temperatūra Farenheitu: " << tempFahrenheit << endl;

    cout << "Įveskite temperatūrą Farenheitu: ";
    cin >> tempFahrenheit;
    fahrenheitToCelsius(tempFahrenheit, tempCelsius);
    cout << "Temperatūra Celsijumi: " << tempCelsius << endl;

    return 0;
}
*/
